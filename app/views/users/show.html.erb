<h2 class="text-center">Welcome to <%= @user.username %>'s page</h2>
<div class="row">
	<div class="text-center">
		<%= gravatar_for @user, size: 150 %>
	</div>
</div>
<h4 class="text-center margin-bottom"><%= @user.username %>'s articles</h4>
<%= render 'articles/articles', obj: @user.articles %>
